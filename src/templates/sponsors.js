import React from "react";
// import PropTypes from "prop-types";

const Sponsors = props => {
  return <div></div>;
};

// Sponsors.PropTypes = {
//   imgLink: PropTypes.oneOfType([PropTypes.object, PropTypes.string]),
//   imgTitle: PropTypes.string
// };

export default Sponsors;
